# Nginx http-context zones for the public ArchiveOS edge. Keep this file in
# /etc/nginx/conf.d so it is evaluated inside the global http block.
limit_req_zone $binary_remote_addr zone=archiveos_public_per_ip:10m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=archiveos_public_conn:10m;
