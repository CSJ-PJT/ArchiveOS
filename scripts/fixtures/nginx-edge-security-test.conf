events {}

http {
    include /etc/nginx/conf.d/archiveos-edge-rate-zones.conf;

    server {
        listen 8080;
        server_name archiveos.test;
        include /etc/nginx/snippets/archiveos-edge-security.conf;

        location / {
            return 200 "ok\n";
        }
    }
}
